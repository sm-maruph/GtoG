/**
 * The demo module uses localStorage through InsContext/store.js so it is ready
 * before the production API exists. Replace these local operations with the
 * endpoints documented in docs/INSURANCE_API_CONTRACT.md when the backend is ready.
 */
export const INSURANCE_API_BASE = '/ins';
